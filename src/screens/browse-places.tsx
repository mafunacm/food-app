import React from "react";
import { View, Text } from "react-native";
import PageContainer from "../reusable-components/page-container";

const EatingPlaces = () => {
    return (
        <PageContainer>
          <Text>Eating Places</Text>
        </PageContainer>
      );
  };

export default EatingPlaces;